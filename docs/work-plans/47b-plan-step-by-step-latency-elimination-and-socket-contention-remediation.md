# خطة العمل 47: التشخيص والاختبار المعياري خطوة بخطوة لجذور بطء استجابة البوت
## Step-by-Step Latency Elimination, Socket Contention & Transport Acceleration Protocol

بناءً على طلب المستخدم المباشر والتزامه المنهجي بجلسة الاستجواب المعمارية (/grill-me)، والقياس الفعلي اللحظي لشاشة مرصد الأداء (APM) واللوحة الميدانية، تهدف هذه الخطة إلى اختبار أسباب بطء الاستجابة سبباً تلو الآخر بشكل تجريبي موثق بالأرقام والمللي ثانية، بدءاً بالقراءة المرجعية (Baseline) ثم قياس أثر كل تحسين على حدة.

---

### 📊 1. قراءة الأساس المرجعية المعتمدة (The Baseline Reading)
- **التاريخ والتوقيت الميداني:** 2026/09/16 - 04:42:47 م
- **الإجراء المرجعي المعتمد للاختبار:** زر 👥 قطاع الموارد البشرية والعمالة (cb:menu:domain:hr)
- **الأرقام المرصودة في الداشبورد وقاعدة البيانات:**
  * **زمن الاستجابة الكلي:** 929 ms (مستوى: بطيء 🐢)
  * **زمن المعالجة الداخلية الصافي (Logic & DB):** 42 ms (4.5% من الزمن فقط — فائق السرعة)
  * **زمن شبكة تليجرام الصافي (Telegram Network):** 887 ms (95.5% من الزمن — عنق الزجاجة الرئيسي)

---

### 🔬 2. فرضية التجربة رقم 1 (Hypothesis 1: Socket Contention & Native Fetch)
- **المشكلة المشخصة:**
  1. عند نقر الزر، يقوم البوت بإطلاق طلبين متزامنين لـ Telegram API في نفس الميلي ثانية: nswerCallbackQuery (في الخلفية)، و editMessageText (من المعالج).
  2. نظراً لأن عميل النقل الافتراضي في grammY هو 
ode-fetch على بروتوكول HTTP/1.1، فإن المقبس الدافئ يُحجز لـ nswerCallbackQuery، مما يجبر editMessageText على فتح اتصال TCP جديد وعمل مصافحة تشفير TLS 1.3 جديدة بالكامل تستغرق وحدها بين 600ms إلى 750ms من مصر إلى أوروبا.
  3. تفعيل عميل النقل الأصلي لـ Node.js 22 (globalThis.fetch) مع مهايئ AbortSignal المخصص، وإلغاء تنافس المقابس للأزرار الموضعية، سيسمح لـ editMessageText باستغلال السوكت الدافئ فوراً وتقليص زمن الشبكة بشكل ملحوظ.

---

### 🛠️ 3. خطوات التنفيذ للاختبار رقم 1
1. **تحديث عميل createBot في pps/bot-server/src/bot.ts:**
   - حقن مهايئ 
ativeFetchAdapter الذي يربط grammY مباشرة بـ globalThis.fetch مع تحويل إشارات الإلغاء المتوافقة مع Node 22.
2. **منع تنافس السوكت في صمام الـ Callback Query (Pre-Routing Instant ACK Gate):**
   - استثناء أزرار التنقل الموضعي السريع (menu:domain:*, menu:hr_sub:*, ction:main_menu) من إطلاق origAnswer() المتوازي في نفس الميلي ثانية، والاعتماد على التعديل الموضعي الفوري editMessageText الذي يلغي مؤشر التحميل تلقائياً في تطبيق تليجرام دون فتح مقبس ثانٍ.
3. **إعادة بناء وتشغيل حاوية البوت وطلب قياس المقارنة الفوري من المستخدم.**
