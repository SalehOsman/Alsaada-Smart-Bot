# المحور 14: الإصدارات وسلسلة التوريد
## Releases & Supply Chain Audit

> **تاريخ التدقيق:** 2026-09-23  
> **المرجعية:** ميثاق `GEMINI.md` البند 4 و 5 (Gate G15, G17, G18)، ونظام Changesets  
> **المدقق:** المهندس المعماري المؤسسي والمدقق التقني المستقل (`/saleh`)

---

### 1. الدرجة والوزن
- **الدرجة:** **74 / 100**
- **الوزن المستخدم في الحساب:** **4**
- **المساهمة في الدرجة الإجمالية:** **2.96%**

---

### 2. الخلاصة
تعتمد إدارة الإصدارات الداخلية على منظومة Changesets الآلية المحكمة، والتي تضمن تزامن أرقام الإصدارات عبر كامل الحزم الـ 14 والموديولات والتطبيقات مع ملف `package.json` الرئيسي وسجل التغييرات التراكمي (`pnpm release:verify` تؤكد التزامن مع الإصدار `2.0.0-alpha.86`). كما يتم فرض التثبيت المغلق للتبعيات في مسار CI (`--frozen-lockfile=true`). نقطة الضعف تكمن في ثغرات سلسلة التوريد المكتشفة في مكتبات الطرف الثالث، حيث سجل `pnpm audit` وجود 12 ثغرة أمنية نشطة.

---

### 3. التغطية
- **المكونات والملفات المفحوصة:**
  - بيان المستودع الرئيسي: [`package.json`](file:///f:/Alsaada-Smart-Bot/package.json).
  - ملف قفل التبعيات: [`pnpm-lock.yaml`](file:///f:/Alsaada-Smart-Bot/pnpm-lock.yaml).
  - سكريبت مزامنة الإصدارات: [`tools/release/sync-root-version.ts`](file:///f:/Alsaada-Smart-Bot/tools/release/sync-root-version.ts).
  - مدقق اتساق الإصدارات: [`tools/governance/verify-release-sync.ts`](file:///f:/Alsaada-Smart-Bot/tools/governance/verify-release-sync.ts).
  - مدقق النظافة البرمجية: [`tools/governance/verify-git-hygiene.ts`](file:///f:/Alsaada-Smart-Bot/tools/governance/verify-git-hygiene.ts).
- **أوامر التحقق المنفذة:**
  - `pnpm release:verify` (اجتاز بنجاح: الإصدار النشط `2.0.0-alpha.86`).
  - `pnpm git-hygiene:verify` (اجتاز بنجاح: فحص 14 حزمة وتطابق كامل دون تلويث للشجرة).
  - `pnpm audit` (خروج برمز خطأ Exit 1: رصد 12 ثغرة أمنية، 5 منها بمستوى High).

---

### 4. تقييم الجوانب الفرعية

| الجانب الفرعي | الحالة | الملاحظات والنتيجة |
|:---|:---:|:---|
| **اتساق وتزامن إصدارات الحزم (Version Parity)** | **سليم بنسبة 100%** | جميع الحزم الـ 14 تحمل نفس الإصدار المتزامن بدقة. |
| **قفل التبعيات والنزاهة التشفيرية** | **سليم بنسبة 100%** | التثبيت محكوم بـ `pnpm-lock.yaml` المعتمد في CI. |
| **سجلات التغيير والـ Changesets** | **سليم** | اعتماد `@changesets/cli` لتوثيق التغييرات قبل كل رفع إصدار. |
| **أمن التبعيات وخلوها من الثغرات (Audit)** | **يحتاج تحسينًا** | وجود 12 ثغرة أمنية في التبعيات العابرة. |
| **آلية التراجع السريع عند فشل النشر** | **يحتاج تحسينًا** | توفر أدوات rollback في `tools/upgrade` لكنها لم تُختبر في CI. |

---

### 5. النتائج المفصلة

#### نتيجة 14.1: تراكم ثغرات أمنية في التبعيات العابرة (Dependency Vulnerabilities)
- **الوصف والأثر:** كشف فحص `pnpm audit` عن وجود 5 ثغرات أمنية بمستوى High و7 بمستوى Moderate، من أبرزها:
  - ثغرة Path Traversal في `@vitest/mocker`.
  - ثغرة استنفاذ الذاكرة في `deepmerge-ts`.
  - ثغرة في حزمة `mysql2` المضمنة عابراً في متطلبات Prisma الداخلية.
- **مستوى الخطورة:** **Major**
- **حالة الدليل:** **مؤكدة** (مخرج تشغيل `pnpm audit --audit-level=high`).
- **بوابة المشروع المرتبطة:** Gate G16, Gate G17.

---

### 6. الحلول المقترحة
- تفعيل حقل `pnpm.overrides` في `package.json` الرئيسي لترقية التبعيات العابرة إلى الإصدارات المصححة دون انتظار ترقية الحزم الأبوية.

---

### 7. المخاطر المتبقية
- إمكانية استغلال الثغرات في بيئات البناء والتطوير المشتركة.

---

### 8. الأولويات
1. تطبيق التجاوزات (Overrides) وإغلاق ثغرات `pnpm audit` (الجهد: 1 ساعة).
